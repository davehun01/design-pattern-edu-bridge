class BitmapRenderer implements Renderer {
    @Override
    public void //TODO finish function def
        System.out.println("We draw a square with a side length of " + side + " using bitmap rendering");
    }
}